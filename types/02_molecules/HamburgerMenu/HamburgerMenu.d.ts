import React, { ReactNode } from 'react';
export interface HamburgerMenuProps {
    children: ReactNode;
}
declare const HamburgerMenu: (props: HamburgerMenuProps) => React.JSX.Element;
export default HamburgerMenu;
