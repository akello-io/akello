services:  
  supertokens:
    extends:
      file: ./environments/dev/docker-compose.yml
      service: supertokens

  supertokens-db:
    extends:
      file: ./environments/dev/docker-compose.yml
      service: supertokens-db

networks:
  supertokens_network:
    driver: bridge      