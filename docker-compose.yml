version: '3'

services:
  # PostgreSQL database for Supertokens
  db-supertokens:
    extends:
      file: ./environments/dev/docker-compose.yml
      service: db-supertokens
  # Supertokens server
  supertokens:
    extends:
      file: ./environments/dev/docker-compose.yml
      service: supertokens
  auth-api:
    extends:
      file: ./environments/dev/docker-compose.yml
      service: auth-api
  auth-app:
    extends:
      file: ./environments/dev/docker-compose.yml
      service: auth-app    
networks:
  app_network:
    driver: bridge