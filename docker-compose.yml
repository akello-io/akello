version: '3'

services:  
  db-supertokens:
    extends:
      file: ./environments/dev/docker-compose.yml
      service: db-supertokens
    
  supertokens:
    extends:
      file: ./environments/dev/docker-compose.yml
      service: supertokens
    
  consul:
    extends:
      file: ./environments/dev/docker-compose.yml
      service: consul
      
  auth-api:
    extends:
      file: ./environments/dev/docker-compose.yml
      service: auth-api
    
  auth-app:
    extends:
      file: ./environments/dev/docker-compose.yml
      service: auth-app    

networks:
  app_network:
    driver: bridge