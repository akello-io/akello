version: '3'

services:
  neuro-service:
    environment:
      POETRY_CACHE_DIR: /app/.cache/pypoetry
    build:
      context: ../../
      dockerfile: microservices/neurology/Dockerfile
    ports:
      - "8011:8000"
    volumes:
      - ../../microservices/neurology:/app/microservices/neurology
    extra_hosts:
      - "host.docker.internal:host-gateway"
    networks:
      - app_network
    depends_on:
      - kafka-ui

networks:
  app_network:
    external: true
